url = "https://fund.cnyes.com/api/v1/fund/B16%2C009/nav?format=table&page="
for num in range(1, 20):
    get_url = url + str(num)
    res = requests.get(get_url)
    content = res.text
    soup = BeautifulSoup(content, 'lxml')
    data = json.loads(content)
    webdatas = data["items"]
    # for products in webdatas:
    for x in range (10):
        print(data["items"]["data"][x]["tradeDate"])
        print(data["items"]["data"][x]["nav"])
#     print(get_url)
